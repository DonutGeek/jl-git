{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "branch-compare",
  "description": "Capability for read-only branch comparison windows",
  "windows": ["branch-compare-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}

# 共享 / 默认构建配置（所有 mode 都会加载，再被 .env.[mode] 覆盖）。
# 本机覆盖可复制为 `.env` 或 `.env.local`（勿提交）。
#
# 仓库内按环境提交：
#   .env.development — `pnpm dev` / `pnpm tauri dev`
#   .env.production  — `pnpm build` / `pnpm tauri build`
#
# Vite 加载顺序（后者覆盖前者）：
#   .env → .env.local → .env.[mode] → .env.[mode].local
#
# 仅允许 VITE_*（会打进前端产物）。禁止放 API Key、token、私钥。

# 应用中文展示名
VITE_APP_NAME_ZH=鲸灵Git
# 应用英文展示名
VITE_APP_NAME_EN=JLGit
# 外部 HTTP 网关前缀（可选）。空则 api 层使用完整 URL（如 DeepSeek）
# VITE_API_BASE_URL=
